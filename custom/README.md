# Custom Artifacts

`custom/` is reserved for local user additions. The installer can copy artifacts from this directory, but repository updates should not overwrite user-created content here.

Use a plugin when an artifact should be shared with other users.
